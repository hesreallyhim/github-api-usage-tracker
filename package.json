{
  "name": "github-api-usage-tracker",
  "private": false,
  "author": "Really Him",
  "license": "MIT",
  "repository": "https://github.com/hesreallyhim/github-api-usage-tracker",
  "type": "commonjs",
  "scripts": {
    "bundle": "rm -rf dist && ncc build src/pre.js -o dist/pre && ncc build src/post.js -o dist/post && ncc build src/noop.js -o dist/noop"
  },
  "dependencies": {
    "@actions/core": "1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "0.38.0"
  },
  "engines": {
    "node": ">=20"
  }
}
